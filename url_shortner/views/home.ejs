<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body{
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background:grey;
            color:aliceblue;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>url shortner application </title>
</head>
<body>
   <h1>URL SHORTNER</h1>

    <% if (locals.id) { %>
        <p> URL generated  https://localhost:8001/url/<%=id%></p>
    <% } %>

    <div>
        <form method="post" action="/url">
            <label for="">Enter your original URL  </label>
            <input type="text" placeholder="http://www.google.com" name="url">
            <button type="submit">Generate short link</button>
        </form>
    </div>

    <% if (locals.urls) { %>
         <table>
            <thead>
                <th>s.no</th>
                <th>short id</th>
                <th>cliced</th>
            </thead>
            <tbody>
                <% urls.forEach((url,index) => { %>
                <tr>
                    <td><%= index+1 %></td>
                    <td><%= url.shortId %></td>
                    <td><%= url.redirectURL %></td>
                    <td><%= url.visitHistory.length %></td>
                </tr>
                <% }) %>
            </tbody>
         </table>   
    <% } %>

</body>
</html>